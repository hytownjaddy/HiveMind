/** Primary navigation; the canonical grouped sidebar (UI-SYSTEM §1) replaces this in the shell task. */
export const NAV_ITEMS = [
  { href: "/", label: "Control Center" },
  { href: "/settings", label: "Settings" },
] as const;
