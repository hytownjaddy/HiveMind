---
id: HM-WO-0005
template: lesson.update
title: "Gold lesson review: ten surgical changes before approval"
status: review_required
priority: high
execution: external
target:
  kind: lesson
  lesson_id: HM-LESSON-linux-networking-01
requested_by: jacob@0pass.com
created_at: 2026-09-09T20:41:26Z
updated_at: 2026-09-09T20:46:23Z
instructions: |-
  Jacob's technical and instructional review of HM-LESSON-linux-networking-01 (2026-09-09): 8.5/10, structure, writing, practical relevance, assessment quality, and lab philosophy PASS; technical precision REVIEW; skill granularity and operational safety CHANGE REQUIRED; claim verification PENDING; scope and cognitive load MINOR REVISION. Not approved yet. Make these surgical changes; do not rewrite the lesson.

  1. Skill model is too coarse. Split linux.networking.routing_table into child skills with routing_table as the parent: linux.networking.routing.lookup (LPM, metrics), .inspect (reading state, ip route get, source/interface lookup), .static_routes (add/replace/del, route types, persistence), .gateway_reachability (on-link nexthop rule), .return_path (missing return routes, when asymmetry matters). Map each objective and question to the specific child skill; an objective may carry several skills.
  2. Gateway claim is too absolute. Canonical claim: by default Linux requires a `via` gateway to resolve as directly reachable on the selected link; normally established by a connected prefix, but an explicit link-scope route or the `onlink` option can establish or override it. Keep the beginner line "for now, think: your gateway needs to be on your local segment".
  3. Connected-route claim needs "normally": ip address supports noprefixroute, which suppresses the automatic prefix route. Do not teach noprefixroute here.
  4. Remove "replace is the safe atomic way to change a default route". ip-route(8) only promises "change or add new one". Write instead that replace changes an existing route or adds it if absent, avoiding delete-then-add; that it does NOT make changing a remote host's default route operationally safe; an incorrect nexthop removes the management path; validate the nexthop and have a console or rollback path first. Add an operational-safety callout there.
  5. Metadata overstates asymmetry as a failure. Asymmetric routing is not inherently broken; it matters when the return route is missing, a stateful middlebox expects symmetry, or strict rp_filter rejects the source path. Reword summary and objective to "diagnose missing return routes and understand when asymmetric paths interact with strict reverse-path filtering".
  6. Fifty minutes is aggressive. Split the estimate: instruction 35, guided lab 20, independent practice 20, total 75.
  7. Move ip_forward and rp_filter out to a later "Routing failure modes" lesson; keep only a short callout: if the table is right and traffic still fails, forwarding policy, filtering, neighbor resolution, and return paths can still be responsible; diagnosed later.
  8. Reflection model answer should be operator-like: observe, localize, hypothesize. Outbound lookup is correct, so establish whether the request reaches the far side and whether a reply leaves; inspect 10.1.2.3's route to my source; capture at the endpoints or the relevant hop to see where return traffic disappears. Match the misconception section's tcpdump method.
  9. Claims: verification becomes structured (status, method documentation|executable|expert, verified_at, reviewer, test id) and sources carry a locator (section) not just a document id. Mark claims HiveMind can execute later (default = 0/0, lower metric preference, ip route get behaviour, blackhole/unreachable, invalid nexthop rejection, connected prefix creation) with method executable and a planned test id; leave status unverified until executed. Apply the source-selection rule: implementation behaviour -> iproute2/kernel docs; OS configuration -> vendor docs (add Red Hat's static-route documentation for persistence); protocol behaviour -> RFC; teaching order -> secondary.
  10. Independent lab: declare grader constraints explicitly: required (host can reach the target, route lookup matches the expected path), preserve (default route, interface addresses), reject (overly broad route, deletion of required routes).

  Also update COURSE_AUTHORING.md with the golden-lesson checklist, the operational-safety callout rule (any command that can sever management access, delete data, change security state, or create blast radius), the "explain simply without stating the simplified model as universally true" rule, the plausible-distractor requirement for questions, the source-selection rule, and the structured verification format. Record this review in metadata.yaml review notes; keep qa_state technical_review for Jacob's re-check.
context:
  repository_paths:
    - content/courses/linux/networking/modules/01-routing/lessons/01-routing-table-ip-route
    - content/sources/
  schemas:
    - schemas/Lesson.schema.json
    - schemas/Question.schema.json
    - schemas/Claim.schema.json
  acceptance_criteria:
    - The requested change is made in lesson.md and, where needed, questions.yaml and claims.yaml.
    - metadata.yaml bumps the lesson version and resets qa_state to draft (invariants 6, 10).
    - All twelve RFP §110 sections remain present and tagged.
    - "`hivemind content compile content/` passes with no errors."
  validation_commands:
    - bun run hivemind -- content compile content/
    - bun run verify
  expected_output:
    - Updated lesson files with a version bump
    - A change report via `hivemind work complete`
  source_requirements:
    - Cite only sources recorded under content/sources (D-011); add a source record before citing it.
    - "Paid books and courses are notes-only: never reproduce their text (invariant 11)."
    - Every factual claim gets an entry in claims.yaml with at least one source id.
labels:
  - content
  - gold-lesson
dependencies: []
validation_runs:
  - at: 2026-09-09T20:46:22Z
    command: bun run hivemind -- content compile content/ && bun run verify
    exit_code: 0
    summary: bun run hivemind -- content compile content/ → exit 0; bun run verify → exit 0
change_report:
  written_at: 2026-09-09T20:45:37Z
  summary: "Applied Jacob's ten review changes to HM-LESSON-linux-networking-01 (v0.2.0): five child skills under linux.networking.routing_table with objectives and questions remapped; gateway and connected-route claims made precise (on-link/onlink, normally); the replace passage rewritten with an operational-safety callout; asymmetry reworded as 'when it matters'; estimate split 35/20/20 = 75; ip_forward and rp_filter reduced to a pointer callout; operator-style reflection answer; structured claim verification with locators, executable test ids, and the Red Hat static-route source; grader constraints declared on both labs. Contracts: Claim 2.0.0, Lesson 2.0.0, SkillDefinition/SkillGraph 1.1.0 (parent, part_of); compiler, repository, web rail, fixtures, and Python contracts updated. COURSE_AUTHORING.md gains the golden-lesson checklist, safety and source-selection rules, and the verification format. qa_state stays technical_review for Jacob's re-check."
  files_changed:
    - .hivemind/work-orders/HM-WO-0005.md
    - COURSE_AUTHORING.md
    - apps/web/components/lesson/RightRail.tsx
    - content/courses/linux/networking/modules/01-routing/lessons/01-routing-table-ip-route/claims.yaml
    - content/courses/linux/networking/modules/01-routing/lessons/01-routing-table-ip-route/lesson.md
    - content/courses/linux/networking/modules/01-routing/lessons/01-routing-table-ip-route/metadata.yaml
    - content/courses/linux/networking/modules/01-routing/lessons/01-routing-table-ip-route/questions.yaml
    - content/skills/linux/networking.routing.gateway_reachability.yaml
    - content/skills/linux/networking.routing.inspect.yaml
    - content/skills/linux/networking.routing.lookup.yaml
    - content/skills/linux/networking.routing.return_path.yaml
    - content/skills/linux/networking.routing.static_routes.yaml
    - content/skills/linux/networking.routing_table.yaml
    - content/sources/linux/redhat-rhel-static-routes.yaml
    - packages/core/src/content/compiler/__fixtures__/invalid/content/courses/linux/demo-course/modules/01-basics/lessons/01-first/claims.yaml
    - packages/core/src/content/compiler/__fixtures__/invalid/content/courses/linux/demo-course/modules/01-basics/lessons/01-first/metadata.yaml
    - packages/core/src/content/compiler/__fixtures__/invalid/content/courses/linux/demo-course/modules/01-basics/lessons/02-second/metadata.yaml
    - packages/core/src/content/compiler/__fixtures__/valid/content/courses/linux/demo-course/modules/01-basics/lessons/01-first/claims.yaml
    - packages/core/src/content/compiler/__fixtures__/valid/content/courses/linux/demo-course/modules/01-basics/lessons/01-first/metadata.yaml
    - packages/core/src/content/compiler/__fixtures__/valid/content/courses/linux/demo-course/modules/01-basics/lessons/02-second/metadata.yaml
    - packages/core/src/content/compiler/compiler.test.ts
    - packages/core/src/content/compiler/index.ts
    - packages/core/src/db/content.ts
    - packages/schema/src/content.ts
    - packages/schema/src/contracts.ts
    - packages/schema/src/fixtures/index.ts
    - packages/schema/src/skills.ts
    - packages/schema/src/sources.ts
    - schemas/Claim.schema.json
    - schemas/Lesson.schema.json
    - schemas/SkillDefinition.schema.json
    - schemas/SkillGraph.schema.json
    - schemas/contracts.lock.json
    - schemas/fixtures/Claim/lpm.json
    - schemas/fixtures/Lesson/routing-table.json
    - services/lab-worker/hivemind_worker/contracts/claim.py
    - services/lab-worker/hivemind_worker/contracts/lesson.py
    - services/lab-worker/hivemind_worker/contracts/skill_definition.py
    - services/lab-worker/hivemind_worker/contracts/skill_graph.py
  commit: daf61bc6eaf8
history:
  - at: 2026-09-09T20:41:26Z
    from: null
    to: draft
    by: jacob@0pass.com
  - at: 2026-09-09T20:41:28Z
    from: draft
    to: exported
    by: jacob@0pass.com
    note: exported for Claude Code
  - at: 2026-09-09T20:41:29Z
    from: exported
    to: in_progress
    by: jacob@0pass.com
    note: pulled by hivemind work pull
  - at: 2026-09-09T20:45:38Z
    from: in_progress
    to: implemented
    by: jacob@0pass.com
    note: change report written
  - at: 2026-09-09T20:46:23Z
    from: implemented
    to: review_required
    by: jacob@0pass.com
    note: "validation passed: bun run hivemind -- content compile content/ && bun run verify"
---

Execute HiveMind Work Order HM-WO-0005.
Follow CLAUDE.md, COURSE_AUTHORING.md, LAB_AUTHORING.md, and the referenced schemas. Execution mode: external (no server-side agent; you are the executor).

## Task

- Title: Gold lesson review: ten surgical changes before approval
- Template: `lesson.update`
- Target: lesson `HM-LESSON-linux-networking-01`
- Priority: high

## Instructions

Jacob's technical and instructional review of HM-LESSON-linux-networking-01 (2026-09-09): 8.5/10, structure, writing, practical relevance, assessment quality, and lab philosophy PASS; technical precision REVIEW; skill granularity and operational safety CHANGE REQUIRED; claim verification PENDING; scope and cognitive load MINOR REVISION. Not approved yet. Make these surgical changes; do not rewrite the lesson.

1. Skill model is too coarse. Split linux.networking.routing_table into child skills with routing_table as the parent: linux.networking.routing.lookup (LPM, metrics), .inspect (reading state, ip route get, source/interface lookup), .static_routes (add/replace/del, route types, persistence), .gateway_reachability (on-link nexthop rule), .return_path (missing return routes, when asymmetry matters). Map each objective and question to the specific child skill; an objective may carry several skills.
2. Gateway claim is too absolute. Canonical claim: by default Linux requires a `via` gateway to resolve as directly reachable on the selected link; normally established by a connected prefix, but an explicit link-scope route or the `onlink` option can establish or override it. Keep the beginner line "for now, think: your gateway needs to be on your local segment".
3. Connected-route claim needs "normally": ip address supports noprefixroute, which suppresses the automatic prefix route. Do not teach noprefixroute here.
4. Remove "replace is the safe atomic way to change a default route". ip-route(8) only promises "change or add new one". Write instead that replace changes an existing route or adds it if absent, avoiding delete-then-add; that it does NOT make changing a remote host's default route operationally safe; an incorrect nexthop removes the management path; validate the nexthop and have a console or rollback path first. Add an operational-safety callout there.
5. Metadata overstates asymmetry as a failure. Asymmetric routing is not inherently broken; it matters when the return route is missing, a stateful middlebox expects symmetry, or strict rp_filter rejects the source path. Reword summary and objective to "diagnose missing return routes and understand when asymmetric paths interact with strict reverse-path filtering".
6. Fifty minutes is aggressive. Split the estimate: instruction 35, guided lab 20, independent practice 20, total 75.
7. Move ip_forward and rp_filter out to a later "Routing failure modes" lesson; keep only a short callout: if the table is right and traffic still fails, forwarding policy, filtering, neighbor resolution, and return paths can still be responsible; diagnosed later.
8. Reflection model answer should be operator-like: observe, localize, hypothesize. Outbound lookup is correct, so establish whether the request reaches the far side and whether a reply leaves; inspect 10.1.2.3's route to my source; capture at the endpoints or the relevant hop to see where return traffic disappears. Match the misconception section's tcpdump method.
9. Claims: verification becomes structured (status, method documentation|executable|expert, verified_at, reviewer, test id) and sources carry a locator (section) not just a document id. Mark claims HiveMind can execute later (default = 0/0, lower metric preference, ip route get behaviour, blackhole/unreachable, invalid nexthop rejection, connected prefix creation) with method executable and a planned test id; leave status unverified until executed. Apply the source-selection rule: implementation behaviour -> iproute2/kernel docs; OS configuration -> vendor docs (add Red Hat's static-route documentation for persistence); protocol behaviour -> RFC; teaching order -> secondary.
10. Independent lab: declare grader constraints explicitly: required (host can reach the target, route lookup matches the expected path), preserve (default route, interface addresses), reject (overly broad route, deletion of required routes).

Also update COURSE_AUTHORING.md with the golden-lesson checklist, the operational-safety callout rule (any command that can sever management access, delete data, change security state, or create blast radius), the "explain simply without stating the simplified model as universally true" rule, the plausible-distractor requirement for questions, the source-selection rule, and the structured verification format. Record this review in metadata.yaml review notes; keep qa_state technical_review for Jacob's re-check.

## Repository paths

- content/courses/linux/networking/modules/01-routing/lessons/01-routing-table-ip-route
- content/sources/

## Schemas

- schemas/Lesson.schema.json
- schemas/Question.schema.json
- schemas/Claim.schema.json

## Acceptance criteria

- The requested change is made in lesson.md and, where needed, questions.yaml and claims.yaml.
- metadata.yaml bumps the lesson version and resets qa_state to draft (invariants 6, 10).
- All twelve RFP §110 sections remain present and tagged.
- `hivemind content compile content/` passes with no errors.

## Validation commands

- `bun run hivemind -- content compile content/`
- `bun run verify`

## Expected output

- Updated lesson files with a version bump
- A change report via `hivemind work complete`

## Source requirements

- Cite only sources recorded under content/sources (D-011); add a source record before citing it.
- Paid books and courses are notes-only: never reproduce their text (invariant 11).
- Every factual claim gets an entry in claims.yaml with at least one source id.

## When done

- Run every validation command; do not weaken an acceptance criterion to pass (invariant 13).
- Do not publish content; approval and publishing stay with Jacob (invariant 10).
- Record the change report: `bun run hivemind -- work complete HM-WO-0005 --summary "<what changed>"`.
